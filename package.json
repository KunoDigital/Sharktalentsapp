{
  "name": "sharktalents",
  "version": "0.1.0",
  "description": "SharkTalents - Plataforma de evaluación de candidatos",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "dependencies": {
    "docx": "^9.6.1",
    "typescript": "^6.0.2"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.17.5"
  }
}
