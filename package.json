{
  "name": "sharktalentsapp",
  "version": "0.1.0",
  "description": "SharkTalents — plataforma multi-tenant de evaluación de talento (refactor v2)",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix shark",
    "dev:frontend": "npm run dev --prefix shark",
    "build:frontend": "npm run build --prefix shark",
    "build:backend": "npm run build --prefix functions/api",
    "build": "npm run build:backend && npm run build:frontend",
    "install:all": "npm install --prefix functions/api && npm install --prefix shark"
  }
}
